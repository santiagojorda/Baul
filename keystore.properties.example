# Copiá este archivo a keystore.properties (gitignored) y completá con tus datos reales.
# storeFile es relativo a la raíz del proyecto.
storeFile=release.jks
storePassword=
keyAlias=baul
keyPassword=
